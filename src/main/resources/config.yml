#Thanks for installing Kozers
#Do not incorrectly format this file, it will get replaced with the default upon parse failure.
#Ex. Deleting lines
#The values can be "incorrect" though, and nothing will get deleted.

#Messages we use
messages:
  prefix:
    string: "<#050505>[<#0055ff>Kozers<#050505>]"
    #Colors are normal hex colors surrounded by angled brackets, and they act like normal minecraft color codes

#Plugin settings
settings:

  #Remember, don't delete!
  debug: true
